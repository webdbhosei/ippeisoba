<%= form_for(@solvability_pro) do |f| %>
  <% if @solvability_pro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solvability_pro.errors.count, "error") %> prohibited this solvability_pro from being saved:</h2>

      <ul>
      <% @solvability_pro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :solvability_id %><br>
    <%= f.number_field :solvability_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
