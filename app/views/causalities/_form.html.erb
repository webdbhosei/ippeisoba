<%= form_for(@causality) do |f| %>
  <% if @causality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@causality.errors.count, "error") %> prohibited this causality from being saved:</h2>

      <ul>
      <% @causality.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  	<%= f.text_field :src_id %>

  	<%= f.label :src_id , [Event.all] %>
  </div>
  <div class="field">
    <%= f.label :src_id, "Source:" %><br>
    <% if @direction == 'src' %>
      <%= f.collection_select :src_id, [Event.find(@event_id)], :id, :title %>
    <% else %>
      <%= f.collection_select :src_id, Event.all, :id, :title %>
    <% end %>
  </div>
  <div class="field">
  	<%= f.label :dst_id, "Destination" %><br>
    <% if @direction == 'dst' %>
      <%= f.collection_select :dst_id, [Event.find(@event_id)], :id, :title %>
    <% else %>
      <%= f.collection_select :dst_id, Event.all, :id, :title %>
    <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
